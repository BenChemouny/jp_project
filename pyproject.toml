[project]
name = "jp-project"
version = "0.1.0"
description = "Voice streaming ASR workspace"
requires-python = ">=3.11"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["client", "server"]
